# Spell word lists (machine-local)
spell/

# macOS metadata
.DS_Store

# Vim swap / backup files (we disable these in options.lua, but just in case)
*.swp
*.swo
*~

# Local Lua LSP config (machine-specific paths)
.luarc.json
.luarc.jsonc

# NOTE: lazy-lock.json is intentionally tracked in this personal config repo.
# It pins exact plugin versions so reinstalling on a new machine gives the
# same environment. Uncomment the line below only if you want to opt out.
# lazy-lock.json
